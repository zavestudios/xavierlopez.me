name: Deploy Jekyll site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy Jekyll Site
    uses: zavestudios/platform-pipelines/.github/workflows/jekyll-deploy.yml@c0aa80bb405fa24ede5ebcf1c40b0be90328386c
