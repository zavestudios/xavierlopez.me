name: Security Scan

on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * 1" # Mondays 9:00 UTC

permissions:
  contents: read
  pull-requests: read

jobs:
  security:
    name: Security Scan
    uses: zavestudios/platform-pipelines/.github/workflows/security-scan.yml@c0aa80bb405fa24ede5ebcf1c40b0be90328386c
